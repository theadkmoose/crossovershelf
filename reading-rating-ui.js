/* Deprecated compatibility stub.
 * Personal rating UI is owned by reading-global-sync.js and My Reading.
 * Kept as an empty file so older cached/index references cannot recreate a
 * second state store, interval, or duplicate modal.
 */
(() => { "use strict"; })();
