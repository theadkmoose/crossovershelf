/* Crossover Shelf — r/Fantasy rank movement */
(() => {
  "use strict";
  const KEY = "crossover-shelf-rfantasy-ranking-v3";
  const FLAG = "__CrossoverShelfRankMovementLoadedV3";
  if (window[FLAG]) return;
  window[FLAG] = true;
  const esc=s=>String(s??"").replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
  const activeTab=()=>document.querySelector('.tab-btn.active,[role="tab"][aria-selected="true"],.tab-btn[aria-current="page"]');
  const isFantasy=()=>/r\/fantasy/i.test(activeTab()?.textContent||"");
  const currentList=()=>{const books=window.CrossoverShelfBooks||[];const tab=activeTab()?.dataset?.tab;return tab?books.filter(b=>b.tab===tab):[]};
  const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||"null")}catch(_){return null}};
  const write=list=>{try{localStorage.setItem(KEY,JSON.stringify({savedAt:Date.now(),positions:Object.fromEntries(list.map((b,i)=>[String(b.id),{rank:i+1,title:b.title}]))}))}catch(_) {}};
  const card=()=>{let el=document.getElementById("rfRankMovement");if(el)return el;const row=document.querySelector(".stats-row");if(!row)return null;el=document.createElement("section");el.id="rfRankMovement";el.className="stat-card rf-rank-movement";el.innerHTML='<h3 role="button" tabindex="0" aria-expanded="true">What\'s Changed?</h3><div class="stat-list" id="rfRankMovementList"></div>';row.appendChild(el);const h=el.querySelector("h3");const toggle=()=>{const c=el.classList.toggle("is-collapsed");h.setAttribute("aria-expanded",String(!c))};h.addEventListener("click",toggle);h.addEventListener("keydown",e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();toggle()}});return el};
  const render=()=>{const el=card();if(!el)return;if(!isFantasy()){el.style.display="none";return}el.style.display="";const current=currentList(),list=document.getElementById("rfRankMovementList");if(!list)return;if(!current.length){list.innerHTML='<div class="rf-empty">No r/Fantasy ranking data.</div>';return}const previous=read();if(!previous?.positions){write(current);list.innerHTML='<div class="rf-empty">Baseline saved. Changes will appear after the next ranking update.</div>';return}const up=[],down=[],stable=[],fresh=[];current.forEach((book,i)=>{const now=i+1,old=previous.positions[String(book.id)];if(!old){fresh.push({book,now});return}const delta=old.rank-now,item={book,now,old:old.rank,delta};if(delta>1)up.push(item);else if(delta<-1)down.push(item);else stable.push(item)});up.sort((a,b)=>b.delta-a.delta);down.sort((a,b)=>a.delta-b.delta);stable.sort((a,b)=>Math.abs(a.delta)-Math.abs(b.delta)||a.now-b.now);fresh.sort((a,b)=>a.now-b.now);const row=(x,cls,symbol,value)=>`<li class="rf-movement-row ${cls}"><span class="stat-rank">${symbol}</span><span class="rf-movement-title">${esc(x.book.title)}</span><span class="stat-val">${value}</span></li>`;const group=(title,rows,cls)=>rows.length?`<div class="rf-movement-group ${cls}"><div class="rf-movement-label">${title}</div><ul>${rows.slice(0,5).join("")}</ul></div>`:"";const html=group("↑ Biggest Rank Climbers",up.map(x=>row(x,"up","↑",`#${x.old} → #${x.now}  ↑${x.delta}`)),"movement-up")+group("↓ Biggest Rank Fallers",down.map(x=>row(x,"down","↓",`#${x.old} → #${x.now}  ↓${Math.abs(x.delta)}`)),"movement-down")+group("✦ New to r/Fantasy",fresh.map(x=>row(x,"new","NEW",`#${x.now}`)),"movement-new")+group("＝ Holding Steady",stable.map(x=>row(x,"stable","＝",`#${x.old} → #${x.now}`)),"movement-stable");list.innerHTML=html||'<div class="rf-empty">No ranking changes since the previous snapshot.</div>'};
  const style=()=>{if(document.getElementById("rf-rank-movement-style"))return;const s=document.createElement("style");s.id="rf-rank-movement-style";s.textContent=`.rf-rank-movement{margin-top:12px}.rf-rank-movement .stat-list{padding-top:4px}.rf-movement-group{margin:7px 0 12px}.rf-movement-label{font:600 .65rem 'IBM Plex Mono',monospace;letter-spacing:.08em;color:var(--text-dim);text-transform:uppercase;margin:0 0 5px}.rf-movement-group ul{list-style:none;margin:0;padding:0}.rf-movement-row{display:flex;align-items:center;gap:8px;min-height:30px}.rf-movement-row .stat-rank{min-width:30px}.rf-movement-title{flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.rf-movement-row .stat-val{white-space:nowrap}.rf-movement-row.up .stat-val{color:var(--brass)}.rf-movement-row.down .stat-val{color:var(--text-dim)}.rf-movement-row.new .stat-rank{font-size:.58rem;color:var(--brass)}.rf-movement-row.stable .stat-val{color:var(--text-dim)}.rf-empty{color:var(--text-dim);font-size:.82rem;line-height:1.45;padding:4px 0 8px}`;document.head.appendChild(s)};
  const boot=()=>{style();setTimeout(render,50);document.addEventListener("click",e=>{if(e.target.closest(".tab-btn,.bottom-nav button"))setTimeout(render,50)});let n=0;const t=setInterval(()=>{if(document.querySelector(".stats-row")){render();clearInterval(t)}else if(++n>30)clearInterval(t)},100)};
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",boot,{once:true});else boot();
})();
